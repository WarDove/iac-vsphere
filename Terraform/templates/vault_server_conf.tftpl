ui = true

listener "tcp" {
  address       = "0.0.0.0:8200"
  cluster_address  = "${ipv4_addr}:8201"
  tls_disable      = true
}
storage "consul" {
  address = "127.0.0.1:8500"
  path    = "vault/"
}
api_addr = "http://${ipv4_addr}:8200"
cluster_addr = "https://${ipv4_addr}:8201"
