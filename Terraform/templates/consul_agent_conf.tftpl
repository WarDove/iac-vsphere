server = false

node_name = "${node_name}"

datacenter = "consul-cluster"

data_dir = "/opt/consul"

bind_addr = "${ipv4_addr}"

client_addr = "127.0.0.1"

retry_join = ${retry_join}

log_level = "DEBUG"

enable_syslog = true

acl_enforce_version_8 = false
